<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/BackgroundPrimary"
    tools:context=".CallActivity">

    <!-- Header / Status -->
    <TextView
        android:id="@+id/tvTargetId"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:text="+81 17-6742-047"
        android:textColor="@color/TextPrimary"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tvDuration"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="00:35"
        android:textColor="@color/TextTertiary"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvTargetId" />

    <!-- Language Info Container -->
    <LinearLayout
        android:id="@+id/layoutLanguage"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvDuration">

        <!-- Me Language -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Me"
                android:textColor="@color/TextTertiary"
                android:textSize="12sp"
                android:layout_marginBottom="4dp"/>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnSourceLang"
                style="@style/Widget.Material3.Button.TonalButton"
                android:layout_width="wrap_content"
                android:layout_height="48dp"
                android:text="English"
                android:textAllCaps="false"
                android:textColor="@color/TextPrimary"
                app:backgroundTint="@color/BackgroundTertiary"
                app:cornerRadius="24dp"
                app:icon="@drawable/ic_launcher_foreground"
                app:iconSize="18dp"
                app:iconTint="@color/TextPrimary" />
        </LinearLayout>

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginHorizontal="8dp"
            android:layout_marginTop="16dp"
            android:src="@android:drawable/ic_popup_sync"
            app:tint="@color/TextTertiary" />

        <!-- Other Person Language -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Other person"
                android:textColor="@color/TextTertiary"
                android:textSize="12sp"
                android:layout_marginBottom="4dp"/>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnTargetLang"
                android:layout_width="wrap_content"
                android:layout_height="48dp"
                android:text="Japanese"
                android:textAllCaps="false"
                android:textColor="@color/TextPrimary"
                app:backgroundTint="@color/AccentPrimary"
                app:cornerRadius="24dp"
                app:icon="@drawable/ic_launcher_foreground"
                app:iconSize="18dp"
                app:iconTint="@color/TextPrimary" />
        </LinearLayout>
    </LinearLayout>

    <!-- Chat / Transcription Area -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvTranscription"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginVertical="16dp"
        android:paddingHorizontal="16dp"
        app:layout_constraintBottom_toTopOf="@+id/layoutControls"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/layoutLanguage" />

    <!-- Bottom Controls -->
    <LinearLayout
        android:id="@+id/layoutControls"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="32dp"
        android:gravity="center"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal">

            <!-- Speaker Toggle -->
            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/fabSpeaker"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="12dp"
                android:src="@android:drawable/ic_lock_silent_mode_off"
                app:backgroundTint="@color/BackgroundTertiary"
                app:fabSize="normal"
                app:tint="@color/TextPrimary" />

            <!-- Spacer for Dial Icon removal -->
            <View
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:layout_marginHorizontal="12dp" />

            <!-- Mute Toggle -->
            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/fabMute"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="12dp"
                android:src="@android:drawable/ic_btn_speak_now"
                app:backgroundTint="@color/BackgroundTertiary"
                app:fabSize="normal"
                app:tint="@color/TextPrimary" />

        </LinearLayout>

        <!-- End Call Button -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabEndCall"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:src="@android:drawable/ic_menu_call"
            app:backgroundTint="@color/StatusError"
            app:fabSize="normal"
            app:tint="@color/TextPrimary" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>